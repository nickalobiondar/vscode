## Summary

<!-- one or two sentences: what does this PR change? -->

## Details

- [ ] `make build` passes (Go + Rust)
- [ ] `make test` passes (`go test -race`, `cargo test`)
- [ ] `gofmt` / `rustfmt` / `clippy` clean
- [ ] trace format change? additive-only, mirrored in both toolchains, `docs/FORMAT.md` updated
- [ ] `make demo` sample diff attached if output changed

## Reproducibility note

<!-- replay/infer output is byte-stable: include before/after on samples/ if behavior changed -->

# draft note 15
